{
  "name": "wh.com",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch": "parcel src/*.pug",
    "build": "parcel build src/now.pug src/index.pug -d public/ --no-cache --no-source-maps --no-autoinstall --no-content-hash",
    "deploy": "firebase deploy",
    "ship": "yarn run build && yarn run deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lost": "^8.3.0",
    "normalize.css": "^8.0.0",
    "postcss-nested": "^4.1.0",
    "posthtml-extend": "^0.3.0",
    "posthtml-include": "^1.2.0",
    "posthtml-md": "^1.1.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.4",
    "pug": "^2.0.3",
    "sugarss": "^2.0.0",
    "tailwindcss": "^0.6.6"
  }
}
